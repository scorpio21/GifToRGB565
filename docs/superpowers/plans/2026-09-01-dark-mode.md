# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir tema oscuro a la interfaz de GifRGB565GUI con persistencia de preferencia.

**Architecture:** Clase estática `ThemeManager` con colores predefinidos, método `ApplyTheme(Form, bool dark)` que recorre todos los controles. Menú "Ver" → "Tema" para alternar. Persistencia en `last_output.json`.

**Tech Stack:** C# WinForms, .NET 8.0

## Global Constraints

- Target: .NET 8.0-windows (WinForms)
- No dependencias externas
- Persistencia en `last_output.json` (mismo archivo que ya existe)
- Colores: fondo #1e1e1e, texto #d4d4d4, acentos #007acc

---

### Task 1: Crear ThemeManager.cs

**Files:**
- Create: `GifRGB565GUI/ThemeManager.cs`

**Interfaces:**
- Produces: `ThemeManager.ApplyTheme(Form form, bool dark)`, `ThemeManager.IsDark`

- [ ] **Step 1: Crear archivo ThemeManager.cs**

```csharp
using System.Drawing;
using System.Windows.Forms;

namespace GifRGB565GUI
{
    public static class ThemeManager
    {
        public static bool IsDark { get; private set; } = false;

        // Colores Dark
        private static readonly Color DarkBg = Color.FromArgb(30, 30, 30);
        private static readonly Color DarkFg = Color.FromArgb(212, 212, 212);
        private static readonly Color DarkAccent = Color.FromArgb(0, 122, 204);
        private static readonly Color DarkControl = Color.FromArgb(45, 45, 48);
        private static readonly Color DarkMenuBg = Color.FromArgb(37, 37, 38);

        // Colores Light (default)
        private static readonly Color LightBg = SystemColors.Control;
        private static readonly Color LightFg = SystemColors.ControlText;
        private static readonly Color LightAccent = SystemColors.Highlight;
        private static readonly Color LightControl = SystemColors.Window;
        private static readonly Color LightMenuBg = SystemColors.MenuBar;

        public static void ApplyTheme(Form form, bool dark)
        {
            IsDark = dark;
            Color bg = dark ? DarkBg : LightBg;
            Color fg = dark ? DarkFg : LightFg;
            Color ctrl = dark ? DarkControl : LightControl;
            Color menuBg = dark ? DarkMenuBg : LightMenuBg;

            form.BackColor = bg;
            form.ForeColor = fg;

            foreach (Control c in form.Controls)
                ApplyToControl(c, dark, bg, fg, ctrl, menuBg);
        }

        private static void ApplyToControl(Control c, bool dark, Color bg, Color fg, Color ctrl, Color menuBg)
        {
            if (c is MenuStrip ms)
            {
                ms.BackColor = menuBg;
                ms.ForeColor = fg;
                foreach (ToolStripMenuItem item in ms.Items)
                    ApplyToMenuItem(item, dark, fg, menuBg);
                return;
            }

            if (c is TextBox || c is ListBox)
            {
                c.BackColor = ctrl;
                c.ForeColor = fg;
            }
            else if (c is PictureBox || c is ProgressBar)
            {
                c.BackColor = dark ? Color.FromArgb(51, 51, 51) : SystemColors.Control;
            }
            else if (c is Button || c is CheckBox)
            {
                if (dark)
                {
                    c.BackColor = Color.FromArgb(62, 62, 66);
                    c.ForeColor = fg;
                }
                else
                {
                    c.BackColor = SystemColors.Control;
                    c.ForeColor = SystemColors.ControlText;
                }
            }
            else if (c is Label || c is TrackBar)
            {
                c.BackColor = bg;
                c.ForeColor = fg;
            }
        }

        private static void ApplyToMenuItem(ToolStripMenuItem item, bool dark, Color fg, Color menuBg)
        {
            item.ForeColor = fg;
            item.BackColor = menuBg;
            foreach (ToolStripMenuItem sub in item.DropDownItems)
                ApplyToMenuItem(sub, dark, fg, menuBg);
        }
    }
}
```

- [ ] **Step 2: Compilar y verificar**

Run: `dotnet build`
Expected: 0 errores

- [ ] **Step 3: Commit**

```bash
git add GifRGB565GUI/ThemeManager.cs
git commit -m "feat: ThemeManager para dark mode"
```

---

### Task 2: Añadir menú "Ver" → "Tema" en Designer

**Files:**
- Modify: `GifRGB565GUI/Form1.Designer.cs`

**Interfaces:**
- Consumes: `ThemeManager.ApplyTheme(Form, bool)`

- [ ] **Step 1: Añadir campos de menú**

En la sección de campos (al lado de `ayudaToolStripMenuItem`):

```csharp
private System.Windows.Forms.ToolStripMenuItem verToolStripMenuItem;
private System.Windows.Forms.ToolStripMenuItem temaToolStripMenuItem;
private System.Windows.Forms.ToolStripMenuItem claroToolStripMenuItem;
private System.Windows.Forms.ToolStripMenuItem oscuroToolStripMenuItem;
```

- [ ] **Step 2: Añadir menú en InitializeComponent()**

Justo después de la inicialización de `ayudaToolStripMenuItem`:

```csharp
verToolStripMenuItem = new ToolStripMenuItem();
temaToolStripMenuItem = new ToolStripMenuItem();
claroToolStripMenuItem = new ToolStripMenuItem();
oscuroToolStripMenuItem = new ToolStripMenuItem();
```

En `menuStrip1.Items.AddRange`, añadir `verToolStripMenuItem`:

```csharp
menuStrip1.Items.AddRange(new ToolStripItem[] { archivoToolStripMenuItem, verToolStripMenuItem, compresionToolStripMenuItem, utilidadesToolStripMenuItem, ayudaToolStripMenuItem });
```

Configurar el menú:

```csharp
// 
// verToolStripMenuItem
// 
verToolStripMenuItem.Name = "verToolStripMenuItem";
verToolStripMenuItem.Size = new Size(35, 20);
verToolStripMenuItem.Text = "Ver";
// 
// temaToolStripMenuItem
// 
temaToolStripMenuItem.Name = "temaToolStripMenuItem";
temaToolStripMenuItem.Size = new Size(160, 22);
temaToolStripMenuItem.Text = "Tema";
temaToolStripMenuItem.DropDownItems.AddRange(new ToolStripItem[] { claroToolStripMenuItem, oscuroToolStripMenuItem });
// 
// claroToolStripMenuItem
// 
claroToolStripMenuItem.Name = "claroToolStripMenuItem";
claroToolStripMenuItem.Size = new Size(160, 22);
claroToolStripMenuItem.Text = "Claro";
claroToolStripMenuItem.Click += claroToolStripMenuItem_Click;
// 
// oscuroToolStripMenuItem
// 
oscuroToolStripMenuItem.Name = "oscuroToolStripMenuItem";
oscuroToolStripMenuItem.Size = new Size(160, 22);
oscuroToolStripMenuItem.Text = "Oscuro";
oscuroToolStripMenuItem.Click += oscuroToolStripMenuItem_Click;
```

- [ ] **Step 3: Compilar y verificar**

Run: `dotnet build`
Expected: 0 errores

- [ ] **Step 4: Commit**

```bash
git add GifRGB565GUI/Form1.Designer.cs
git commit -m "feat: menú Ver → Tema en Designer"
```

---

### Task 3: Integrar tema en Form1.cs

**Files:**
- Modify: `GifRGB565GUI/Form1.cs`

**Interfaces:**
- Consumes: `ThemeManager.ApplyTheme(Form, bool)`, `ThemeManager.IsDark`

- [ ] **Step 1: Añadir handlers de menú y carga de tema**

Al final de `Form1.cs`, antes del cierre de la clase:

```csharp
private void claroToolStripMenuItem_Click(object? sender, EventArgs e)
{
    ThemeManager.ApplyTheme(this, false);
    ClearCheckmarks(temaToolStripMenuItem);
    claroToolStripMenuItem.Checked = true;
    SaveConfig();
}

private void oscuroToolStripMenuItem_Click(object? sender, EventArgs e)
{
    ThemeManager.ApplyTheme(this, true);
    ClearCheckmarks(temaToolStripMenuItem);
    oscuroToolStripMenuItem.Checked = true;
    SaveConfig();
}

private void ClearCheckmarks(ToolStripMenuItem parent)
{
    foreach (ToolStripMenuItem item in parent.DropDownItems)
        item.Checked = false;
}
```

- [ ] **Step 2: Modificar LoadConfig para cargar tema**

En `LoadConfig()`, añadir después de cargar `lastName`:

```csharp
var themeMatch = Regex.Match(json, "\"theme\"\\s*:\\s*\"(dark|light)\"");
if (themeMatch.Success)
{
    bool dark = themeMatch.Groups[1].Value == "dark";
    ThemeManager.ApplyTheme(this, dark);
    if (dark) oscuroToolStripMenuItem.Checked = true;
    else claroToolStripMenuItem.Checked = true;
}
```

- [ ] **Step 3: Modificar SaveConfig para guardar tema**

En `SaveConfig()`, incluir el tema en el JSON:

```csharp
var theme = ThemeManager.IsDark ? "dark" : "light";
var json = $"{{\"lastName\":\"{lastName}\",\"theme\":\"{theme}\",\"recentFiles\":[{recentJson}]}}";
```

Hacer lo mismo en `SaveRecentFiles`.

- [ ] **Step 4: Compilar y verificar**

Run: `dotnet build`
Expected: 0 errores

- [ ] **Step 5: Commit**

```bash
git add GifRGB565GUI/Form1.cs
git commit -m "feat: integrar dark mode en Form1"
```

---

### Task 4: Test manual y commit final

- [ ] **Step 1: Ejecutar la app**

Run: `dotnet run --project GifRGB565GUI`

Verificar:
- Menú Ver → Tema → Oscuro cambia todos los controles
- Menú Ver → Tema → Claro restaura colores
- Cerrar y abrir → el tema persiste
- Todos los menús, botones, checkboxes, textboxes, listas cambian de color

- [ ] **Step 2: Commit final**

```bash
git add .
git commit -m "feat: dark mode completo con persistencia"
git push origin main
```
