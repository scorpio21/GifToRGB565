# Rescale automático de resolución — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Añadir controles de escalado automático de frames antes de la conversión a RGB565, con opciones predefinidas y entrada manual.

**Architecture:** Controles UI en el formulario (NumericUpDown, ComboBox, Checkbox), método `ResizeFrame()` en Form1, aplicado antes de `ImageConverter.ToRGB565()`.

**Tech Stack:** C# WinForms .NET 8.0-windows

## Archivos afectados

- `Form1.Designer.cs` — Nuevo GroupBox "Rescale" con controles
- `Form1.cs` — Lógica de rescale + persistencia
- `ImageConverter.cs` — Sin cambios (acepta Bitmap de cualquier tamaño)

---

## Task 1: Añadir controles de UI (Designer)

**Files:** `GifRGB565GUI/Form1.Designer.cs`

- [ ] Añadir campos: `grpRescale`, `cmbRescalePreset`, `nudRescaleW`, `nudRescaleH`, `chkKeepRatio`
- [ ] Instanciar controles en `InitializeComponent`
- [ ] Configurar propiedades de cada control
- [ ] Añadir al formulario y posicionar debajo de los checkboxes de filtros (y ~430)

**Grupo de controles (y ~430):**
```
cmbRescalePreset (ComboBox) — Original, 50%, 25%, 160x120, 320x240, Personalizado
lblRescaleX (Label) — "x"
nudRescaleW (NumericUpDown) — 8..2048
nudRescaleH (NumericUpDown) — 8..2048
chkKeepRatio (CheckBox) — "Mantener proporción"
```

---

## Task 2: Lógica de rescale en Form1.cs

**Files:** `GifRGB565GUI/Form1.cs`

- [ ] Método `ResizeFrame(Bitmap original, int targetW, int targetH)` → devuelve `Bitmap` nuevo con `.GetHbitmap()` o `new Bitmap(w, h)` + `Graphics.DrawImage`
- [ ] Método `cmbRescalePreset_SelectedIndexChanged` — cambia nudW/nudH según selección
- [ ] Método `nudRescaleW_ValueChanged` — si chkKeepRatio, calcula alto manteniendo proporción
- [ ] Método `nudRescaleH_ValueChanged` — si chkKeepRatio, calcula ancho manteniendo proporción
- [ ] Integración en `LoadGifFrames`, `LoadFrames`, `cargarHeaderToolStripMenuItem_Click` — aplicar resize a cada frame antes de usar
- [ ] Integración en `btnGenerate_Click` — aplicar resize antes de generar

---

## Task 3: Persistencia y tema

**Files:** `GifRGB565GUI/Form1.cs`

- [ ] Guardar `rescalePreset`, `rescaleW`, `rescaleH`, `keepRatio` en `last_output.json`
- [ ] Cargar en `LoadConfig()`
- [ ] Añadir al método `UpdateStatusBar()` — mostrar dimensiones post-rescale si aplica

---

## Task 4: Compilar, test manual, commit

- [ ] `dotnet build` — 0 errores
- [ ] Probar: cargar GIF, cambiar preset, verificar que frames se escalan
- [ ] Probar: mantener proporción al cambiar ancho
- [ ] Probar: persistencia del tema al reiniciar
- [ ] Commit + push
- [ ] Cerrar issue #6
